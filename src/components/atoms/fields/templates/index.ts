import { TemplateSimple } from "./simple";

// Template namespace — acesso via Input.Template.Simple.
// Um template por componente hoje; namespace preparado para mais.
const Template = {
	Simple: TemplateSimple,
};

export { Template };
